from django.apps import AppConfig

class FinanceBillingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.finance_billing"
    verbose_name = "Municipal Finance, Budgets & Double-Entry Ledgers"
